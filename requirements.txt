fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
# Détection automatique de la pose des personnages (ControlNet)
numpy>=1.24
opencv-python-headless>=4.8
# Optionnel : accélère la détection (repli automatique sur OpenCV DNN si absent)
# onnxruntime>=1.16
